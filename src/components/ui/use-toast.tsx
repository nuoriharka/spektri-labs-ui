// Deprecated: use `@/components/ui/toaster` or `@/components/ui/use-toast` (this file) which re-exports from toaster.
// This shim keeps backwards compatibility and ensures a single implementation.
export { Toaster, useToast } from "./toaster"
